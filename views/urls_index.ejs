<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>TinyApp</title>

</head>
<body style="font-size:20px; padding:50px;">
  <%- include ./partials/_header %> <!--This enables header to show on this page-->

  <% if (user) { %> <!--implimenting different view for if a user is logged in or not:-->

  <ul>
    <% for (let shortURL in urls) { %>
      <li><%= shortURL %> - <%= urls[shortURL] %> </li> <!--previously created shortURLs will diplay here (if user is logged in) along with their corresponding longURLs. If none have been created this section will be blank-->

      <form method="POST" action=<%=`/urls/${shortURL}/delete`%>>
        <button class="btn btn-primary">Delete</button>
        <a href="<%=`/urls/${shortURL}`%>" class="btn btn-primary">Edit</a>  <!--Link added (styled as button) here to allow 'delete' and 'edit' to appear side by side for styling.-->
      </form>

    <% } %>
  </ul>

    <a href="urls/new" > Click here to create new TinyURL! </a>

  <% } else { %>

    <p>
      You need to login or register to view this page!
    </p>

  <% } %>

</body>
</html>